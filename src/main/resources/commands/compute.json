{
	"compute": 
	{
		"description": "Allow to solve a calculation",
		"permissions": ["discord.send_messages", "discord.read_message_history"],
		"arguments": 
		[
			{
				"type": "string...",
				"tag": "calculation",
				"keep_value": true,
				"execute": "compute"
			}
		]
	}
}